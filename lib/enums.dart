//Types of cues
enum CueType {
  intensity,
  color,
  iris,
  out
}

// Adding cue or Inserting Cue
enum AddInsert {
  add,
  insert
}

// Type for blocks. Cue = data, blank = spacer
enum BlockType {
  cue,
  blank
}