import 'package:flutter/material.dart';

class BlockCanvas extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    return Container(
      // Left side has a cue number list
      // BlockCanvas accepts a list of cue numbers in. This builds a UI list on left hand side of rows. Each row is X in height. 100% width


      // children: cueNumbers.map(
      //   (item) => (Text(item.toString())),
      // ),
    );
  }
}